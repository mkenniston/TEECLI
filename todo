	TODO (Tasks)

- doc: clean up "ffi" vs. "adapter" terminology
- add (eval), (display exp ...) to builtins
- add (define sym ex) (if cond ex1 ex2) (if cond ex)
- add (/ ...) variadic, (expt base exponent)
- add > < <= >= = (all variadic)
- add (not exp) (and ...) (or ...)
- add (max ...) (min ...) (abs exp)
- add (append item list)
- add (car exp) (cdr exp) (cons exp exp)
- add (begin ) (let ) (list )
- add (eq? ...) (equal? ..)
- add (number? ) (symbol? ) (string? ) (boolean? ) (null? )
- add (string-xxx ) whole family of methods
- add (lambda )
- add formatting for output (at the string level)

- research the most accepted/popular styles for each target language,
	and set the flags appropriately on the de-linters
- continue w/ Design Philo and Architecture sections of the Manual
? change targets/ dir to vm/
	and ? create a sut/ dir for what gets run (including TEECLI itself)

- add "make" where appropriate, and add to tool-chain (verify)
- figure out how exceptions/call/cc works
- figure out how to implement ffi
- learn to do objects in Racket

- look up exact atom lexical rules, make ESE conform to them
- implement javascript VM
- implement c VM

- add runtimes for (display, eval, quote, cons, car, left, cdr, right, define)
- add macros/forms, to enable simple framework coding

- continue adding languages from wishlist:
	Python, Java, Javascript
	C, C++, C# (.NET)
	Racket, Rust, Go
	Perl, Ruby
 	R, PHP
	Swift, Pascal/Delphi, Visual Basic (.NET), Fortran, Ada
- someday add Windows support, maybe using "mono" (Visual Basic, etc.)
- add whitepapers (2) to github/chronian

- Chronian: look at MATLAB (via Java binding) for how they do datetimes


DONE:
- added E opcode in .ese format
- got the flow running from:
	- input .tee files, piped to
	- the compiler (in Racket), ESE output piped to
	- the Python reader, passed to
	- the Python executer, which prints the original expressions back out
- created a github repo and linked to local copy
- changed name of github repo from teecli to TEECLI since it's an acronym
- added a verify-packages script that verifies all necessary tools
	are installed (full toolchain)
- split SE/Pair/Atom classes, Reader, and Excuter into separate files
- added "ruff" to tool-chain (python de-linter)
- added "shellcheck" to tool-chain (bash de-linter)
- moved title/subtitle/tagline into the new main doc file
- started the master "TEECLI Manual"
- added more toolchain to verify-packages script
- added code to do lexical scoping
- added first part of eval() implementation
- renamed *.tee to *.ebs
- renamed Executer to VM
- renamed all the Expression stuff to use "VM" instead
- added the dispatcher for functions and macros, plus some builtins
- added newline in .ese output from compiler, to make it easier to debug
- added copyright/license block to every source file (and the Manual)
- added more material to the Manual

